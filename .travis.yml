language: python
python:
  - "3.6"
env:
  - PYTHONPATH=.
install:
  - pip install -r requirements.txt
script:
  - ls -a
  - ls -l /home/travis/build/LowellInstruments/Logger/tests
  - pytest -v tests/*.py
